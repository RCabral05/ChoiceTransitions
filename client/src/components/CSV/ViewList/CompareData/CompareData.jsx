import React, { useState, useMemo } from 'react';

const CompareData = ({stateData, sheetData}) => {
    console.log(sheetData);
    console.log(stateData);

  return (
    <>
     
    </>
  );  
};

export default CompareData;
